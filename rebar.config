{erl_opts, [debug_info]}.

{deps, [{ranch, "1.7.1"},
        {recon, "2.5.0"}]}.

{profiles, [{test, [{deps, [meck]}]}]}.

{shell, [
    {config, "./config/shell.config"},
    {apps, [opcua, ranch]}
]}.

{relx, [{release, {opcua, "1.0.0"},
	[opcua, xmerl, recon]},
        {dev_mode, true},
        {include_erts, false},
        {extended_start_script, true}]}.
