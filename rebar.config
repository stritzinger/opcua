{erl_opts, [debug_info]}.
{deps, []}.

{shell, [{apps, [opcua]}]}.

{relx, [{release, {opcua, "1.0.0"},
	[opcua]},
        {dev_mode, true},
        {include_erts, false},
        {extended_start_script, true}]}.
